{
  "name": "entities",
  "version": "1.0.0",
  "main": "dist/index.js",
  "bin": {
    "arkham": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "start": "node dist/cli.js",
    "dev": "npm run clean && npm run build && node dist/cli.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "CLI tool to fetch and format Arkham labels for entities",
  "devDependencies": {
    "@types/commander": "^2.12.0",
    "@types/node": "^20.11.24",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "commander": "^11.1.0"
  }
}